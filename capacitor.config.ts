import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ftmappdev.ftmapp',
  appName: 'FTMApp',
  webDir: 'dist/ftmapp/browser'
};

export default config;
